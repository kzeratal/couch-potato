{
  "name": "couch-potato",
  "version": "0.0.1",
  "description": "Code virtual map generator — shadow-repo metadata overlay for Claude Code",
  "type": "module",
  "bin": {
    "couch-potato": "./bin/couch-potato.ts"
  },
  "scripts": {
    "dev": "bun run bin/couch-potato.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.91.1",
    "ignore": "^7.0.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
